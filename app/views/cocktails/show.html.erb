<h1><%= @cocktail[:name] %></h1>
<ul>
  <% @cocktail.doses.each do |dose|%>
  <li>
    <%= dose.description %>
    <%= dose.ingredient.name %>
  </li>
  <% end %>
</ul>


  <%= link_to cocktails_path, class: "nav-link" do%>
    <i class="fas fa-plus"></i> Back to cocktail list
  <% end %>

  <%= link_to new_cocktail_dose_path(@cocktail), class: "nav-link" do%>
    <i class="fas fa-plus"></i> Add a new dose
  <% end %>

